
const inicialState={ //estado global
    pokemos:[],
};


const rootReducer = (state= inicialState, action)=>{

    switch (action.type) {
        case value:
            
            break;
    
        default:
            return{...state};
    }

}